// Title: Valid Palindrome
            // Difficulty: Easy
            // Language: Python3
            // Link: https://leetcode.com/problems/valid-palindrome/

class Solution:
    def isPalindrome(self, s: str) -> bool:
        low = s.lower()
        l = 0
        r = len(clean) - 1
        while l < r:
            if clean[l] != clean[r]:
                return False
            l+=1
            r-=1
        clean = ''.join(c for c in low if c.isalnum())
        
        print(clean)
        return True
